{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["public/**", ".next/**"],
      "env": ["NODE_ENV"]

    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
