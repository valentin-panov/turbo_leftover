{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**/*", ".next/**/*"],
      "dependsOn": ["^build"],
      "env": ["NODE_ENV"]

    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
