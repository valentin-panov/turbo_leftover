{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**/*", ".next/**/*"],
      "dependsOn": ["^build"],
      "env": ["NODE_ENV"]

    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
